{
  "name": "karts",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "@octokit/rest": "^20.0.2",
    "@google/generative-ai": "^0.21.0",
    "@heyputer/puter.js": "^2.0.0"
  }
}

