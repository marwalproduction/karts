{
  "name": "karts",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "busboy": "^1.6.0",
    "tesseract.js": "^7.0.0"
  }
}

